{
  "name": "brasamar",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo do Brasa e Mar — landing page e futuros apps do restaurante.",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.10.9",
    "typescript": "^5.9.3"
  }
}
